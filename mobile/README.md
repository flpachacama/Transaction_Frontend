# Mobile (React Native + TypeScript)

This folder contains a minimal React Native + TypeScript scaffold.

Quick start (after installing packages):

```bash
cd mobile
npm install
# then run one of:
npm run android
npm run ios
```
